apiVersion: v1
kind: Service
metadata:
  name: victoria-metrics-victoria-metrics-auth-custom
  namespace: victoria-metrics
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8427
  selector:
    app.kubernetes.io/instance: victoria-metrics
    app.kubernetes.io/name: victoria-metrics-auth
