apiVersion: serving.knative.dev/v1alpha1
kind: DomainMapping
metadata:
  name: metrics.code4.ro
  namespace: victoria-metrics
spec:
  ref:
    name: victoria-metrics-grafana
    kind: Service
    apiVersion: v1
