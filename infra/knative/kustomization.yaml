apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - https://github.com/knative/operator/releases/download/knative-v1.10/operator.yaml
  - https://github.com/knative/net-certmanager/releases/download/knative-v1.10/release.yaml

resources:
  - crd.yaml

patchesStrategicMerge:
  - config-certmanager.yaml
