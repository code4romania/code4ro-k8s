apiVersion: serving.knative.dev/v1beta1
kind: DomainMapping
metadata:
  name: api.fiialiat.ro
  namespace: fii-aliat
spec:
  ref:
    name: fii-aliat
    kind: Service
    apiVersion: serving.knative.dev/v1
