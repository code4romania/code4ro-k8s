apiVersion: serving.knative.dev/v1beta1
kind: DomainMapping
metadata:
  name: in.primulrand.ro
  namespace: primulrand
spec:
  ref:
    name: primulrand
    kind: Service
    apiVersion: serving.knative.dev/v1
