apiVersion: serving.knative.dev/v1beta1
kind: DomainMapping
metadata:
  name: app.consilierehiv.ro
  namespace: sens-pozitiv
spec:
  ref:
    name: sens-pozitiv
    kind: Service
    apiVersion: serving.knative.dev/v1
