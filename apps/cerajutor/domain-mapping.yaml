apiVersion: serving.knative.dev/v1beta1
kind: DomainMapping
metadata:
  name: api.cerajutor.ro
  namespace: cerajutor
spec:
  ref:
    name: cerajutor
    kind: Service
    apiVersion: serving.knative.dev/v1
