apiVersion: serving.knative.dev/v1beta1
kind: DomainMapping
metadata:
  name: app.stopviolenteidomestice.ro
  namespace: sunrise
spec:
  ref:
    name: sunrise
    kind: Service
    apiVersion: serving.knative.dev/v1
