apiVersion: serving.knative.dev/v1alpha1
kind: DomainMapping
metadata:
  name: sunrise-api.heroesof.tech
  namespace: sunrise
spec:
  ref:
    name: sunrise
    kind: Service
    apiVersion: serving.knative.dev/v1
