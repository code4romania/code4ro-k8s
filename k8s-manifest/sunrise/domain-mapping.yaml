apiVersion: serving.knative.dev/v1alpha1
kind: DomainMapping
metadata:
  name: sunrise-api.stopviolenteidomestice.ro
  namespace: sunrise
spec:
  ref:
    name: sunrise
    kind: Service
    apiVersion: serving.knative.dev/v1
