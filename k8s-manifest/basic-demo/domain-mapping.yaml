apiVersion: serving.knative.dev/v1alpha1
kind: DomainMapping
metadata:
  name: demo.heroesof.tech
  namespace: basic-demo
spec:
  ref:
    name: basic-demo
    kind: Service
    apiVersion: serving.knative.dev/v1
